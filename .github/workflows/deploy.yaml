name: Deploy Flask Calculator to Render

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Deploy to Render
      run: |
        echo "✅ Code pushed to main branch"
        echo "🚀 Render will auto-deploy from GitHub"
        echo "📝 Make sure your Render service is connected to this repo"